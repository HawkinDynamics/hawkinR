# Validate GetTest Parameters

Check `athleteId`, `testTypeId`, `teamId`, and `groupId` parameters.

## Usage

``` r
ParamValidation(
  arg_athleteId = NULL,
  arg_testTypeId = NULL,
  arg_teamId = NULL,
  arg_groupId = NULL
)
```

## Value

no object returned.
