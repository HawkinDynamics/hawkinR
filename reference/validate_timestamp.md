# Validate time stamp arguments and allow for date character

This function checks `from` and `to` arguments for valid inputs of epoch
or a date as a character string.

## Usage

``` r
validate_timestamp(x)
```

## Arguments

- x:

  A data frame containing flattened athlete test data.

## Value

An epoch time stamp
