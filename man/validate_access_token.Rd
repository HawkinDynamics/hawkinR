% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{validate_access_token}
\alias{validate_access_token}
\title{Retrieve and Validate Stored Access Token}
\usage{
validate_access_token(fn_name = "hawkinR")
}
\arguments{
\item{fn_name}{Calling function label, used for log context.}
}
\value{
The access token (character). Stops with an instructive error when
the token is missing or expired.
}
\description{
Reads the access token and expiration stored by \code{get_access()}, validates
both, and returns the token. A missing, empty, or non-numeric (\code{NA})
expiration is treated as an expired token — prompting re-authentication —
rather than being allowed to propagate \code{NA} into the calling \code{if} condition
(which raises "missing value where TRUE/FALSE needed").
}
\keyword{internal}
